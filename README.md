# EDM Studio Native 0.7.3

Windows 原生 C++20 / Dear ImGui / Direct3D 11 桌面程序。直接打开 DCS EDM v8/v10，预览参数动画、涂装、RoughMet 和动态编号，导出 GLB / glTF 2.0。运行时不需要 Python、Qt、DCS、Blender 或网络。

## 使用

打开 `EDM-Studio.exe`，选择或拖入 EDM。左侧搜索参数或节点名称，调节滑块、输入精确值、播放和复位；右侧选择自动发现的涂装，或导入 `description.lua` / ZIP。保存的游戏重定向目录、DCS / Steam 本体、用户模组及手动扫描目录均参与查找。

涂装贴图支持同一机型目录内的相对引用：例如A.zip中的 `../B/wing` 可读取B.zip中的wing.dds，也可读取B目录中的同名文件；漫反射与RoughMet共用此规则，预览、绘制和导出均适用。

- 左/右键旋转，中键平移，滚轮缩放；F 适应视图，F11 聚焦视口。
- Ctrl+O 打开，Ctrl+E 导出，空格播放所选参数。
- 画面下方“浏览已编辑的涂装”可对照原图；编辑与撤销记录保留，保存/导出仍包含绘制结果。新版工程保留原始颜色贴图，重开也能对照。
- 涂装面板可设置机身编号、动态 Lua JSON 变量；导出面板设置全部参数 / 当前参数 / 当前静态姿态、时长和贴图。
- 预览贴图上限可选 1024/2048/4096/8192，预算默认 2 GB。在“涂装 → 贴图质量”输入 0.25–100 GB，回车或点击“应用贴图预算”，自动保存并重新选择预览 mip。预览设置不影响导出；未编辑贴图保持原始分辨率，绘制贴图按画布实际分辨率输出。
- 设置保存在 EXE 同目录的 `edm-studio.settings.json`。原创示例见 `samples/animation_demo.edm`。

## 动画与材质

EDM 参数范围转换为每参数独立的时间片段，默认 3 秒。保留负参数、层级、定向缩放、四元数、8 权重蒙皮和逆绑定矩阵。每段动画将其他参数固定为导出时的当前值（含涂装 custom_args 和编号），静态模式保存当前组合姿态。

RoughMet 的 R/G/B 对应环境遮蔽/粗糙度/金属度。NumberNode 编号图集转换为标准 STEP 显隐节点。动态 Lua 使用内嵌 Lua 5.4.8 的独立子进程，支持函数、循环、条件及目录范围内的 dofile/require；内存、指令和 5 秒时间上限防止错误配置阻塞界面。

DCS 专用灯光、法线/效果着色器、环境反射、损伤和其他材质参数动画不能完整映射到通用 PBR。内部 LOD 当前全部保留，碰撞壳不加入可见模型；优先使用明确的单个 LOD 文件。未知布局会明确报错。原始格式分析见 [读取与动画导出分析](docs/DCS_EDM_读取与动画导出分析.md)，旧版记录见 [0.3.4 参考](docs/LEGACY_0.3.4.md)。

## 构建与验证

安装 Visual Studio C++ 桌面开发工具和 Windows SDK，使用 PowerShell 7。依赖源码已放入 native/vendor，可离线构建。

根目录 `./build.ps1` 会构建原生程序、运行全部原生测试并打包。分步运行如下：

```powershell
./native/build.ps1
./native/test.ps1 -SkipBuild
./native/package.ps1 -SkipBuild -Source
./build/native/Release/EDM-Studio-Native.exe
./build/native/Release/edm-native-cli.exe inspect 'path/to/model.edm'
./build/native/Release/edm-native-cli.exe export 'path/to/model.edm' --output 'model.glb' --args 0,9,10,38
```

CLI 支持 inspect、export、analyze、catalog、livery，以及 --livery、--context、--baseline、--bort、--static、--duration、--textures、--no-textures。`native/fetch-deps.ps1` 可重新获取固定版本依赖。原 Python 实现保留用于开发对照，原生运行和构建均不调用它。打包脚本输出版本目录及Windows/source ZIP；源码ZIP对应本地Git的HEAD，打包前应提交源代码。

0.7.2的11组原生测试、1350项检查通过，包含工程原图保存、跨ZIP贴图、表面贴花、跨画布事务、自动覆盖、局部UV栅格及异步轨迹。F100完成默认2GB下的2K贴花、编辑前后切换和工程重开验证；F14实测1/2/100GB预算及设置持久化。此前F14、F100的2K图片画笔、撤销顺序及GPU像素回读均通过，相机移动不重建投射深度图或模型几何。F100的Panter涂装跨ZIP机翼引用已实际复现并修复，先前绘制工程双动画GLB通过Khronos校验（0错误、0警告）。各精度、范围及性能结果见[原生验证](docs/NATIVE_VALIDATION.md)，问题和限制见[无人值守报告](docs/UNATTENDED_REPORT.md)。旧版的F/A-18C、C-130动画及Blender回读记录仍保留。

## 许可证与随附源码

项目原创代码、文档、图标和自制测试样例采用 [MIT 许可证](LICENSE)，允许商用、修改、转售与闭源使用，须保留版权和许可声明。第三方代码和改编代码仍遵循各自的许可证，详见 [第三方说明](THIRD_PARTY_NOTICES.md)。程序不包含 DCS 游戏模型、贴图或 DLL。

根目录 [third_party_sources](third_party_sources/README.md) 提供实际使用的 Eigen 3.4.0 完整源码、全部原始许可文件和 SHA256 清单；Windows 便携包也包含该目录，可离线取得。0.7.3 补齐原源码归档被上游 Git 忽略规则漏掉的 167 个 Eigen 文件，并为 ProggyClean 字体和 ImGui 内嵌 stb 单独附上许可。打包会校验工作目录及两个 ZIP 内的 Eigen 源码字节。

0.7.3 将原创飞机图标内嵌到 GUI 和 CLI 的 EXE，窗口和任务栏使用同一图标，运行时无需旁置 ICO。图标包含 16–256 像素的 10 种尺寸；矢量原稿位于 `native/assets/edm-studio.svg`，修改后可在 Windows 下运行 `./native/generate-icon.ps1` 重新生成 ICO。正常编译直接使用仓库中的 ICO，无需额外图像工具。

## 动画部件识别

原生加载后自动后台采样每个参数的实际几何运动，显示部件候选、启发式匹配分、前后左右位置、最大位移及依据。可搜索部件名称、定位和高亮受影响网格。评分不代表统计准确率，不确定时保留未知/候选，不按固定参数编号套名称。

默认最多约12,000个顶点样本；包含关键帧、中间姿态、完整蒙皮。分析时可以显示被其他参数隐藏的部件，真实预览/导出姿态不受影响。26项合成测试通过；F14 221参数约1.79s（10项关键部件断言），F100 90参数约2.43s（11项关键断言）。F14界面实际座舱盖高亮与两组GPU全顶点回读通过，相机移动仍为0几何上传/0动画求值。

## 涂装模板与直接画笔

右侧“绘制”页始终提供“浏览 / 直接画笔 / 贴花 / 相机投影”，无需先选材质或建立画布。选“直接画笔”后在模型上按住左键，软件按落笔位置异步匹配当前涂装的漫反射贴图，笔迹可连续跨越材质和UV接缝。贴图加载期间保留笔迹队列，整笔完成后可一次撤销或重做。右键旋转，中键平移；支持世界半径、硬度、不透明度和保留原透明遮罩。

新画布分辨率上限默认2K，可选4K或8K；切换设置不会重采样已经编辑的画布。可预载PNG/DDS/TGA/JPG等平面模板，下一次落笔位置决定模板对应的贴图，替换的是整张漫反射贴图。有未保存编辑时，替换前先写恢复备份。添加局部图案时使用“贴花”或“相机投影”。PSD图层需先导出平面PNG。

“保存工程与DCS涂装”在所选目录中新建完整文件夹，含PNG工程、带mip的DDS、依赖贴图与description.lua；可将其放入正确机型的Liveries目录，也可在本软件重新导入。GLB/glTF导出包含当前绘制结果。未保存编辑在切换模型/关闭前写入EXE旁PaintRecovery恢复工程；保存失败会保留窗口。恢复工程通过“打开绘制工程”读取。

绘制自动定位漫反射贴图，动态编号图集由编号功能控制，不参与绘画。同名且引用相同漫反射资源的材质别名共用画布；重叠/共用UV仍会联动。若导入的工程含无法用DCS材质名区分的不同图片，完整涂装保存会报告冲突。视口只更新贴图脏区，相机移动不重建拾取结构或模型顶点。遮挡按几何判断，不计算透明贴图孔洞。

自动跨贴图画笔、模板落点绑定、多画布撤销及工程重开已完成真实GPU回读。模板安装即使没有笔刷像素变化，也会正确标记未保存并进入恢复工程。

## 表面贴花 / 图片画笔

在“贴花”中加载透明PNG等图片，鼠标移到模型上即可按落点表面的朝向贴合预览，左键单击盖印。无需选材质、手工建画布或调整圆柱。宽度使用米，支持锁定比例、旋转、不透明度和贴合深度；`[` / `]` 调整大小，`Q` / `E` 旋转。右键旋转模型、中键平移和滚轮缩放视角继续可用。

预览在GPU上投射到实际曲面，未盖印时不修改贴图；最终采用局部正交投射、深度范围、背面筛选与精确遮挡射线，将图案写入对应UV。一次单击作为一个跨贴图撤销步骤，取消或失败整次回滚。默认不连续重复盖印，避免按住鼠标时意外叠加透明度。当前相机投影仍保留独立工具。

## 当前相机图片投影

选择“相机投影”，载入图片后在视口中拖动定位，可锁定比例、调节大小/旋转/透明度。点击应用时固定当前摄像机和姿态，自动寻找图案覆盖的全部漫反射贴图并写入各自UV，无需逐个选择材质。支持几何遮挡、背面过滤、跨贴图统一撤销和取消回滚。预览蒙层可以独立关闭，查看实际烘焙后的效果。

新增自动覆盖测试包括跨近裁剪面、长三角形穿过图片边缘、多个材质共用UV与透明图案只混合一次。整个操作共用时间、像素和射线预算，失败时回滚全部已改贴图。F14/F100自动多贴图投影已通过真实GPU回读。高精度整机投影可能超过时间预算（2K及以上为300秒，较低精度为120秒），此时缩小图片覆盖范围或降低新画布精度。详细步骤见[绘制使用指南](docs/绘制使用指南.md)。

既有工程加载会恢复基础涂装、RoughMet、外观参数和绘制像素。极大笔刷的一次光栅化不能在中途抢占，整幅图片烘焙在后台进行。共享或重叠UV的限制仍然存在，自动定位贴图不会改变原模型拓扑。
